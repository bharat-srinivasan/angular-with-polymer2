import { PxDirective } from './px.directive';

describe('PxDirective', () => {
  it('should create an instance', () => {
    const directive = new PxDirective();
    expect(directive).toBeTruthy();
  });
});
